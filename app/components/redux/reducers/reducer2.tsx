// anotherReducer.js
const initialState = {
    // Define initial state for another reducer if needed
  };
  
  const anotherReducer = (state = initialState, action: any) => {
    // Implement reducer logic for another state if needed
    return state;
  };
  
  export default anotherReducer;
  